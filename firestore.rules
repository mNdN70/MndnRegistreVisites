rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access for the application's functionality.
    // This is necessary because the app does not use Firebase Authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
