rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read access to the users collection for login purposes
    match /users/{userId} {
      allow read;
    }

    // Secure all other collections
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
